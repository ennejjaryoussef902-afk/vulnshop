# VulnShop — GitHub Pages Site

This branch contains the static landing page for VulnShop, served via GitHub Pages.

**Live site:** https://YOUR_USERNAME.github.io/vulnshop

## Structure

```
index.html   ← Single-file landing page (HTML + CSS + JS, no dependencies)
.nojekyll    ← Skips Jekyll processing
_config.yml  ← Minimal GitHub Pages config
```

## Deploying

The site is served automatically from the `gh-pages` branch.
To update, push changes to `gh-pages`.

See the main repository at [github.com/YOUR_USERNAME/vulnshop](https://github.com/YOUR_USERNAME/vulnshop).
